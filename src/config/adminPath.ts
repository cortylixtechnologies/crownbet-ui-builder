// Obscure base path for the admin area so the URL isn't trivially guessable.
// Change this string to rotate the admin URL.
export const ADMIN_BASE = "/cb-control-9k2x";
export const ADMIN_LOGIN = `${ADMIN_BASE}/login`;
